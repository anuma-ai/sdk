# WhisperWorkerOutgoing

> **WhisperWorkerOutgoing** = { `progress`: `number`; `status`: `string`; `type`: `"progress"`; } | { `text`: `string`; `type`: `"result"`; } | { `message`: `string`; `type`: `"error"`; }

Defined in: [src/lib/speechToText/types.ts:13](https://github.com/zeta-chain/ai-sdk/blob/main/src/lib/speechToText/types.ts#L13)

Messages sent FROM the worker
