# UnverifiableReason

> **UnverifiableReason** = `"not-auto-extracted"` | `"no-provenance"` | `"sources-missing"`

Defined in: [src/lib/memory/verifySupport.ts:184](https://github.com/anuma-ai/sdk/blob/main/src/lib/memory/verifySupport.ts#184)

Why a memory could not be checked against its sources at all. Distinct from
a failed check — see this module's header.
