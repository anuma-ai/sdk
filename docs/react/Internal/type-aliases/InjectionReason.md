# InjectionReason

> **InjectionReason** = `"imperative_override"` | `"role_marker_leak"` | `"exfiltration_url"`

Defined in: [src/lib/memory/injectionScreen.ts:37](https://github.com/anuma-ai/sdk/blob/main/src/lib/memory/injectionScreen.ts#37)

Why a candidate was quarantined. Coarse buckets over the signature set
below — surfaced for audit/telemetry, never alongside the content.
