# INTERNAL\_FLOW\_MARKER

> `const` **INTERNAL\_FLOW\_MARKER**: `"Anuma internal first-party flow (not user chat)."` = `"Anuma internal first-party flow (not user chat)."`

Defined in: [src/lib/internalFlowMarker.ts:51](https://github.com/anuma-ai/sdk/blob/main/src/lib/internalFlowMarker.ts#51)

The verbatim marker line. Deliberately semantically inert: it states provenance and
nothing else, so prepending it cannot change how a model answers an extraction or
classification prompt. Do not reword, translate, or interpolate.
