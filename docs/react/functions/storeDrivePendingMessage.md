# storeDrivePendingMessage()

> **storeDrivePendingMessage**(`message`: `string`): `void`

Defined in: [src/lib/auth/google-drive.ts:344](https://github.com/zeta-chain/ai-sdk/blob/main/src/lib/auth/google-drive.ts#L344)

Store a pending message to retry after OAuth completes

## Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `string` |

## Returns

`void`
