# hasGoogleDriveCredentials()

> **hasGoogleDriveCredentials**(): `boolean`

Defined in: [src/lib/backup/google/auth.ts:341](https://github.com/zeta-chain/ai-sdk/blob/main/src/lib/backup/google/auth.ts#L341)

Check if we have any stored credentials (including refresh token)
Works with both encrypted and unencrypted credentials

## Returns

`boolean`
