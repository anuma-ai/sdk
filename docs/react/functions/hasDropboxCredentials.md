# hasDropboxCredentials()

> **hasDropboxCredentials**(): `boolean`

Defined in: [src/lib/backup/dropbox/auth.ts:302](https://github.com/zeta-chain/ai-sdk/blob/main/src/lib/backup/dropbox/auth.ts#L302)

Check if we have any stored credentials (including refresh token)

## Returns

`boolean`
