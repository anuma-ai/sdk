# hasDropboxCredentials()

> **hasDropboxCredentials**(): `boolean`

Defined in: [src/lib/backup/dropbox/auth.ts:304](https://github.com/zeta-chain/ai-sdk/blob/main/src/lib/backup/dropbox/auth.ts#L304)

Check if we have any stored credentials (including refresh token)
Works with both encrypted and unencrypted credentials

## Returns

`boolean`
