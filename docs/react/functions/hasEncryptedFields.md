# hasEncryptedFields()

> **hasEncryptedFields**(`memory`): `boolean`

Defined in: [src/lib/db/memory/encryption.ts:341](https://github.com/zeta-chain/ai-sdk/blob/main/src/lib/db/memory/encryption.ts#L341)

Check if a memory has any encrypted fields.
Useful for determining if migration is needed.

## Parameters

### memory

[`MemoryData`](../interfaces/MemoryData.md)

## Returns

`boolean`
